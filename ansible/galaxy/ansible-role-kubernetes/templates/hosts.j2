#
# File generated by ansible-role-kubernetes 
# {{ ansible_managed }}

127.0.0.1 localhost

# KUBERNETES CONTROL PLANES NODES
{% for node in groups['kube-control-plane'] %}
{{ hostvars[node].ansible_host }} {{ hostvars[node].inventory_hostname }}
{% endfor %}

# KUBERNETES WORKER NODES
{% for node in groups['kube-worker'] %}
{{ hostvars[node].ansible_host }} {{ hostvars[node].inventory_hostname }}
{% endfor %}

